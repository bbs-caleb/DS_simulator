# gp_page_2 — Пошаговое объяснение решения с полного нуля

## 1. Какой файл отправлять

В проверяющую систему нужно загружать:

```text
gp_page_2_solution.py
```

Markdown-файлы нужны для обучения, но не заменяют Python-файл решения.

---

# 2. Полный код

```python
"""Calculate the cumulative gain ranking metric."""

from typing import List

import numpy as np


def cumulative_gain(relevance: List[float], k: int) -> float:
    """Score is cumulative gain at k (CG@k).

    Parameters
    ----------
    relevance : List[float]
        Relevance labels for ranked objects.
    k : int
        Number of first elements to be counted.

    Returns
    -------
    float
        Cumulative gain for the first k elements.
    """
    score = np.sum(relevance[:k])
    return float(score)
```

---

# 3. Первая строка

```python
"""Calculate the cumulative gain ranking metric."""
```

Это docstring всего файла.

Он сообщает:

> В этом модуле вычисляется метрика Cumulative Gain.

Тройные кавычки используются для документации.

Docstring:

- помогает человеку понять назначение файла;
- улучшает качество кода;
- полезен для Pylint;
- является нормальной production-практикой.

---

# 4. Импорт `List`

```python
from typing import List
```

`typing` — модуль стандартной библиотеки Python.

`List` используется в аннотации:

```python
relevance: List[float]
```

Это означает:

> аргумент `relevance` ожидается как список вещественных чисел.

Пример:

```python
[0.99, 0.94, 0.88]
```

Аннотация не выполняет вычисление. Она описывает API.

---

# 5. Импорт NumPy

```python
import numpy as np
```

NumPy — библиотека численных вычислений.

`as np` создаёт короткое общепринятое имя.

После импорта мы можем писать:

```python
np.sum(...)
```

вместо:

```python
numpy.sum(...)
```

---

# 6. Почему между импортами пустая строка

```python
from typing import List

import numpy as np
```

PEP8 делит импорты на группы:

1. стандартная библиотека;
2. сторонние библиотеки;
3. локальные модули.

`typing` — стандартная библиотека.

`numpy` — сторонняя библиотека.

Поэтому между ними оставлена пустая строка.

---

# 7. Объявление функции

```python
def cumulative_gain(relevance: List[float], k: int) -> float:
```

Разберём по элементам.

## `def`

Сообщает Python, что создаётся функция.

## `cumulative_gain`

Точное имя функции из задания.

Менять его нельзя: автотест ищет именно это имя.

## `relevance`

Список оценок в порядке выдачи.

## `List[float]`

Подсказка, что внутри списка находятся вещественные числа.

## `k`

Число первых элементов, которые нужно учесть.

## `int`

Подсказка, что `k` — целое число.

## `-> float`

Подсказка, что функция возвращает вещественное число.

## `:`

Открывает тело функции.

---

# 8. Почему API нельзя менять

Если написать:

```python
def cg(values, top_k):
```

математика может быть правильной, но тесты не найдут функцию.

Если добавить обязательный аргумент:

```python
def cumulative_gain(relevance, k, normalize):
```

тест вызовет функцию с двумя аргументами и получит ошибку.

Поэтому API — часть задания наравне с формулой.

---

# 9. Docstring функции

Блок:

```python
"""Score is cumulative gain at k (CG@k).
...
"""
```

описывает:

- назначение;
- параметры;
- возвращаемое значение.

Это не влияет на число, но важно для читаемости и Pylint.

---

# 10. Главная строка

```python
score = np.sum(relevance[:k])
```

В ней выполняется вся математика.

Разберём справа налево.

---

# 11. Что лежит в `relevance`

Пример:

```python
relevance = [0.99, 0.94, 0.88, 0.74, 0.71, 0.68]
```

Каждый элемент — релевантность объекта на соответствующей позиции.

| Индекс | Позиция | Значение |
|---:|---:|---:|
| 0 | 1 | 0.99 |
| 1 | 2 | 0.94 |
| 2 | 3 | 0.88 |
| 3 | 4 | 0.74 |
| 4 | 5 | 0.71 |
| 5 | 6 | 0.68 |

---

# 12. Что делает `relevance[:k]`

Пусть:

```python
k = 5
```

Тогда:

```python
relevance[:k]
```

равно:

```python
relevance[:5]
```

Результат:

```python
[0.99, 0.94, 0.88, 0.74, 0.71]
```

В Python правая граница среза не включается.

Поэтому `[:5]` берёт индексы:

```text
0, 1, 2, 3, 4
```

Это ровно пять элементов.

---

# 13. Почему не `[:k + 1]`

Новичок может подумать:

> Индексация начинается с нуля, значит нужно прибавить единицу.

Но для среза это неправильно.

```python
relevance[:5]
```

уже берёт пять элементов.

```python
relevance[:6]
```

взяло бы шесть элементов.

---

# 14. Что делает `np.sum`

После среза Python передаёт в NumPy:

```python
np.sum([0.99, 0.94, 0.88, 0.74, 0.71])
```

NumPy выполняет:

```text
0.99 + 0.94 + 0.88 + 0.74 + 0.71
```

Получается:

```python
4.26
```

---

# 15. Что такое `score`

```python
score =
```

создаёт переменную.

После выполнения строки:

```python
score = 4.26
```

Имя `score` означает значение метрики.

Понятное имя лучше, чем:

```python
x
a
res1
```

---

# 16. Возврат результата

```python
return float(score)
```

`np.sum` может вернуть NumPy-тип:

```python
numpy.float64
```

`float(score)` преобразует его в стандартный Python `float`.

`return` передаёт результат вызывающему коду.

---

# 17. Чем `return` отличается от `print`

Неправильно:

```python
print(score)
```

Это только показывает число на экране.

Функция при этом вернёт:

```python
None
```

Правильно:

```python
return float(score)
```

Тогда можно написать:

```python
result = cumulative_gain(relevance, 5)
```

И в `result` будет число.

---

# 18. Полный путь выполнения

Вызов:

```python
relevance = [0.99, 0.94, 0.88, 0.74, 0.71, 0.68]
result = cumulative_gain(relevance, 5)
```

Шаг 1:

```python
relevance[:5]
```

даёт:

```python
[0.99, 0.94, 0.88, 0.74, 0.71]
```

Шаг 2:

```python
np.sum(...)
```

даёт примерно:

```python
4.26
```

Шаг 3:

```python
score = 4.26
```

Шаг 4:

```python
float(score)
```

даёт стандартный Python `float`.

Шаг 5:

```python
return 4.26
```

Шаг 6:

```python
result == 4.26
```

---

# 19. Почему иногда видно `4.260000000000001`

Компьютер хранит вещественные числа в двоичном формате.

Некоторые десятичные дроби нельзя представить абсолютно точно.

Поэтому иногда возможен вывод:

```python
4.260000000000001
```

Это не логическая ошибка.

Разница чрезвычайно мала. Тесты для float обычно сравнивают с допуском.

Мы не округляем результат, потому что условие этого не требует.

---

# 20. Проверка примера

```python
relevance = [0.99, 0.94, 0.88, 0.74, 0.71, 0.68]
k = 5

print(cumulative_gain(relevance, k))
```

Ожидаемый результат:

```text
4.26
```

---

# 21. Дополнительные тесты

## Только первый элемент

```python
cumulative_gain([0.8, 0.7, 0.6], 1)
```

Берётся:

```python
[0.8]
```

Ответ:

```python
0.8
```

## Первые два

```python
cumulative_gain([0.8, 0.7, 0.6], 2)
```

Ответ:

```python
1.5
```

## `k = 0`

```python
cumulative_gain([0.8, 0.7], 0)
```

Срез пустой, ответ:

```python
0.0
```

## Пустой список

```python
cumulative_gain([], 5)
```

Ответ:

```python
0.0
```

## `k` больше длины

```python
cumulative_gain([0.8, 0.7], 10)
```

Берутся все доступные элементы, ответ:

```python
1.5
```

---

# 22. Почему список не сортируется

Входной список уже отражает фактический порядок модели.

Пример:

```python
relevance = [0.1, 0.1, 1.0]
k = 2
```

Правильный CG:

\[
0.1 + 0.1 = 0.2
\]

Если ошибочно отсортировать:

```python
[1.0, 0.1, 0.1]
```

получится:

\[
1.0 + 0.1 = 1.1
\]

Это оценка не фактической, а изменённой выдачи.

---

# 23. Почему не считается среднее

CG — сумма:

\[
CG@k = \sum rel_i
\]

Среднее было бы:

\[
\frac{\sum rel_i}{k}
\]

Это уже другая метрика.

Поэтому нельзя использовать:

```python
np.mean(...)
```

---

# 24. Почему нет логарифма

Логарифм появится в DCG.

Текущая задача — именно CG.

Если добавить:

```python
rel / np.log2(position + 1)
```

получится другая метрика, и автотесты упадут.

---

# 25. Почему нет нормализации

Нормализация появится в nDCG.

Сейчас не нужно:

- делить на идеальный результат;
- сортировать;
- вычислять IDCG;
- ограничивать значение диапазоном 0–1.

Нужно только сложить первые `k` оценок.

---

# 26. Почему код не округляет

Неправильно:

```python
return round(float(score), 2)
```

Если настоящий ответ:

```python
0.123456
```

округление вернёт:

```python
0.12
```

Тест может ожидать исходную точность.

---

# 27. Почему нет лишней валидации

Можно было бы добавить много проверок:

- тип списка;
- тип каждого элемента;
- неотрицательность `k`;
- границы relevance;
- длину списка.

Но условие этого не требует.

В автопроверяемой задаче дополнительное поведение может нарушить скрытый тест.

Минимальное решение безопаснее и точнее следует контракту.

---

# 28. Почему используется `np.sum`, а не цикл

Через цикл:

```python
score = 0.0

for value in relevance[:k]:
    score += value
```

Это тоже правильно.

Но `np.sum`:

- короче;
- яснее;
- использует шаблонный импорт;
- содержит меньше мест для ошибки.

---

# 29. PEP8-разбор

## Имена

```python
cumulative_gain
```

использует `snake_case`.

## Отступы

Внутри функции — четыре пробела.

## Пустые строки

Перед функцией верхнего уровня — две пустые строки.

## Импорты

Стандартные и сторонние импорты разделены.

## Docstrings

Есть документация модуля и функции.

## Длина строк

Нет ненужных сверхдлинных строк.

---

# 30. Почему решение соответствует требованиям API

Сохранено точное объявление:

```python
def cumulative_gain(relevance: List[float], k: int) -> float:
```

Функция:

- называется правильно;
- принимает два аргумента;
- использует правильный порядок;
- возвращает число;
- не печатает вместо возврата;
- не меняет входной список.

---

# 31. Что может проверять Pylint

Pylint может обращать внимание на:

- отсутствие module docstring;
- отсутствие function docstring;
- неправильное имя;
- неиспользуемый импорт;
- форматирование;
- слишком длинные строки.

В решении:

- docstrings присутствуют;
- `numpy` используется;
- имена понятные;
- форматирование аккуратное.

---

# 32. Почему решение рассчитано на 100 баллов

80% за корректность:

- формула реализована точно;
- `k` используется;
- берутся первые элементы;
- возвращается сумма.

20% за качество:

- PEP8;
- docstrings;
- type hints;
- понятное имя переменной;
- стандартный `float`;
- отсутствие лишнего кода.

---

# 33. Что нужно запомнить навсегда

Чтобы взять первые `k` элементов:

```python
relevance[:k]
```

Чтобы сложить:

```python
np.sum(relevance[:k])
```

Чтобы вернуть обычный `float`:

```python
return float(score)
```

Вся задача словами:

> Возьми первые k релевантностей, сложи их и верни сумму.
